# CLI Contracts

- The CLI is Ferrify's operator shell for the governed agent runtime.
- Human output should summarize policy, plan, patch scope, and verification evidence.
- JSON output should remain machine-readable and map directly to Rust report types.
- Error messages should make it obvious whether failure came from approval rules, invalid input, or verification.
